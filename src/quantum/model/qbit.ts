


  export class QState {
    // ¡Esto es lo que necesitas añadir!
    //public amplitude: (state: number) => { real: number; imag: number };
    //public numBits: number;
  

  }

  /*
  function obtenerAmplitudes(estado: QState): Array<{ real: number; imag: number }> {
  const amplitudes: Array<{ real: number; imag: number }> = [];
  for (let i = 0; i < Math.pow(2, estado.numBits); i++) {
    amplitudes.push(estado.amplitude(i)); // Acceso directo
  }
  return amplitudes;
}

  */