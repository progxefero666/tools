


export class CmOperation {
   static OPID_UNDEFINED = "undefined";
   static OPID_OK= "OK";
   static OPID_GENERATE = "Generate";
   static OPID_HOME = "home";
   static OPID_NEW = "new";
   static OPID_ABOUT = "about";
   static OPID_CONFIRM = "OK";
   static OPID_CANCEL = "Cancel";
   static OPID_CONTINUE = "Continue";
   static OPID_NEXT = "next";
}


/*
export const GenCommands: Record<string, Command> = {
   UNDEFINED: new Command("undefined","undefined"),
   NEW: new Command("new", "New"),
   OK: new Command("ok", "OK"),
   CANCEL: new Command("cancel", "Cancel"),
   CONTINUE: new Command("continue", "Continue"),
   NEXT: new Command("next", "Next")
};

export const NavCommands: Record<string, Command> = {
   HOME: new Command("nav_home", "home"),
   CONTACT: new Command("nav_contact", "contact"),
};
*/
